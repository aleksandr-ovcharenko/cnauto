{
  "build": {
    "env": {
      "FLASK_ENV": "production",
      "FLASK_APP": "backend/app.py"
    }
  },
  "deploy": {
    "startCommand": "cd backend && flask run --host=0.0.0.0 --port=$PORT"
  }
}
